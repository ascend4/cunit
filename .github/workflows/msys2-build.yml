name: CUnit MSYS2 Build

on:
  workflow_dispatch:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  package:
    name: Build MSYS2 package (UCRT64)
    runs-on: windows-latest

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - name: Configure Git line endings
        shell: bash
        run: git config --global core.autocrlf input

      - name: Check out source
        uses: actions/checkout@v4

      - name: Verify VERSION matches PKGBUILD
        id: version
        shell: bash
        run: |
          version_file="$(tr -d '[:space:]' < VERSION)"
          pkg_version="${version_file//-/.}"

          if ! grep -Fq "_distver=\"${version_file}\"" packaging/msys2/PKGBUILD; then
            echo "PKGBUILD _distver does not match VERSION (${version_file})" >&2
            exit 1
          fi

          if ! grep -Eq "^pkgver=${pkg_version//./\\.}$" packaging/msys2/PKGBUILD; then
            echo "PKGBUILD pkgver does not match VERSION (${pkg_version})" >&2
            exit 1
          fi

          echo "dist_name=CUnit-${version_file}" >> "${GITHUB_OUTPUT}"

      - name: Set up MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MSYS
          update: true
          cache: true
          install: >-
            base-devel
            git

      - name: Create source tarball for PKGBUILD
        env:
          DIST_NAME: ${{ steps.version.outputs.dist_name }}
        working-directory: packaging/msys2
        run: |
          git -C ../.. archive \
            --format=tar.gz \
            --prefix="${DIST_NAME}/" \
            -o "${DIST_NAME}.tar.gz" \
            HEAD

      - name: Build package
        working-directory: packaging/msys2
        run: MINGW_ARCH=ucrt64 makepkg-mingw -sCLf --noconfirm

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: cunit-msys2-ucrt64-pkg
          path: packaging/msys2/*.pkg.tar.zst
          if-no-files-found: error

  test-package:
    name: Test installed MSYS2 package (UCRT64)
    runs-on: windows-latest
    needs: package

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - name: Set up MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          cache: true
          install: >-
            mingw-w64-ucrt-x86_64-cc
            mingw-w64-ucrt-x86_64-pkgconf

      - name: Download package artifact
        uses: actions/download-artifact@v4
        with:
          name: cunit-msys2-ucrt64-pkg
          path: pkg

      - name: Install package artifact
        run: |
          pacman -U --noconfirm pkg/*.pkg.tar.zst
          test -f "${MINGW_PREFIX}/include/CUnit/CUnit.h"
          test -f "${MINGW_PREFIX}/lib/libcunit.a" || test -f "${MINGW_PREFIX}/lib/libcunit.dll.a"

      - name: Smoke test installed package
        run: |
          cat > smoke.c <<'EOF'
          #include <CUnit/CUnit.h>

          int main(void) {
            if (CU_initialize_registry() != CUE_SUCCESS) {
              return 1;
            }
            CU_cleanup_registry();
            return 0;
          }
          EOF

          gcc smoke.c -o smoke $(pkgconf --cflags --libs cunit)
          ./smoke
